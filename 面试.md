1. 本イベントの背景・概要
背景

年金給付システムに登録されている住所情報と、J-LIS側で管理されている住所情報に差異が存在していたことにより、一部対象者に対して老齢年金請求書および定期便が未送達となる事象が発生していた。

これまで当該対応については、保守作業として定期的にJ-LIS照会を実施していたが、手作業による運用負荷およびヒューマンエラーリスク低減を目的として、J-LIS照会向けデータ抽出処理のシステム化を実施する。

案件概要

本案件では、個人番号と基礎年金番号の紐付情報を抽出し、J-LIS照会対象データを作成するバッチ処理を新規構築する。

また、既存の異動者情報抽出処理をベースとしつつ、死亡者情報マージ処理を除外したうえで、新規JobNetを追加する構成とする。

加えて、既存AP資材に対して中間ファイル出力制御機能の改修を実施し、既存業務への影響を抑えた構成とする。

夜間バッチにて対象者情報を抽出し、翌営業日の中間バッチにてHULFT連携を行う運用を想定する。

然后你接下来应该追加一个
2. 対応内容

这个特别重要。

因为 kickoff 里：

背景 ≠ 你干什么

很多新人会混。

推荐你这样写
2. 対応内容

① 個人番号－基礎年金番号紐付情報抽出APの新規作成

J-LIS照会対象者向けの紐付情報抽出処理を新規作成する
既存異動者情報抽出処理をベースとする
死亡者情報マージ処理は対象外とする

② 既存AP資材改修

中間ファイル出力要否をジョブ定義パラメータで制御可能とする
出力先切替に対応する

③ JobNet定義情報作成

上記バッチ処理実行用JobNetを新規作成する
夜間バッチ／中間バッチ連携を実施する

④ 設計・テスト

基本設計書／詳細設計書作成
単体テスト
結合テスト
然后成果物（成果物一覧）

你这个其实很像 NTT 风格。









## 案件概要

本案件では、照会対象者データ作成に必要となる、個人番号と基礎年金番号の紐付情報抽出機能を新規作成する。

また、当該処理を定期的に実行するためのJobNet定義情報を新規作成する。

主な対応範囲は、AP資材の新規作成、既存処理の一部改修、JobNet定義情報の作成、および関連設計書・テストの実施とする。

这才是“像做过案件的人写的”。
