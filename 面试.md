/*
 * @(#)NMdI6310201BLogic.java 
 * 
 * Copyright (c) 2016-2018, Japan Pension Service All Rights Reserved.
 */
package jp.go.nenkin.gyomu.n.batch.ni63_1_2.blogic;

import java.io.File;
import java.util.HashMap;
import java.util.Map;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.beans.factory.annotation.Qualifier;

import jp.go.nenkin.gyomu.fw.batch.blogic.BatchBLogicParamEx;
import jp.go.nenkin.gyomu.fw.batch.blogic.BatchBLogicResult;
import jp.go.nenkin.gyomu.fw.batch.blogic.StandardBatchBLogic;
import jp.go.nenkin.gyomu.fw.batch.collector.CollectorEx;
import jp.go.nenkin.gyomu.fw.batch.collector.DBCollectorEx;
import jp.go.nenkin.gyomu.fw.dao.db.DbIteratableQueryDAO;
import jp.go.nenkin.gyomu.fw.log.LogEx;
import jp.go.nenkin.gyomu.fw.log.LogFactoryEx;
import jp.go.nenkin.gyomu.fw.util.file.DirPathCategory;
import jp.go.nenkin.gyomu.fw.util.file.FileAccessUtil;
import jp.go.nenkin.gyomu.n.batch.common.util.NSirkWrkConvUtility;
import jp.go.nenkin.gyomu.n.batch.ni63_1_2.dbdto.SelectHnnnKaknnInfConditionDTO;
import jp.go.nenkin.gyomu.n.batch.ni63_1_2.dbdto.SelectHnnnKaknnInfResultDTO;
import jp.go.nenkin.gyomu.n.batch.ni63_1_2.filedto.NI6310203FileAccessDTO;
import jp.go.nenkin.gyomu.n.batch.ni63_1_2.filedto.NI6310204FileAccessDTO;
import jp.terasoluna.fw.file.dao.FileLineWriter;
import jp.terasoluna.fw.file.dao.FileUpdateDAO;

/**
 * 異動者情報照会の被保険者情報を抽出し、年金給付住基本人確認異動情報取得要求送信を編集する.
 * @author 株式会社NTTデータ
 */
public final class NMdI6310201BLogic extends StandardBatchBLogic<SelectHnnnKaknnInfResultDTO> {

    /** 被保険者保有番号種類(06:仮基礎年金番号） */
    private static final String HHKNSH_HYU_BNG_SHU_KR_KSNNKNBNG = "06";

    /** 被保険者保有番号種類(01:基礎年金番号） */
    private static final String HHKNSH_HYU_BNG_SHU_KSNNKNBNG = "01";

    /** 被保険者保有番号種類(09:個人番号） */
    private static final String HHKNSH_HYU_BNG_SHU_KJNBNG = "09";

    /** 住民票生存状況表示（2：不明） */
    private static final String JMNHY_SIZN_JOKY_HYJI_FMI = "2";

    /** ファイル名（NG00000215） */
    private static final String FILE_NAME_NG00000215 = "NG00000215";

    /** ファイル名（NG00000206） */
    private static final String FILE_NAME_NEF0000206 = "NEF0000206";

    /** ファイル名（JOB分割） */
    private static final String FILE_NAME_SPT = "SPT";

    /** ファイル名（セパレータ） */
    private static final String FILE_NAME_SEP = "_";

    /** ファイル名（拡張子・DAT） */
    private static final String EXT_DAT = ".dat";

    /** ユースケースID:N-I-6-3-1-2 */
    private static final String CASE_N_I_6_3_1_2 = "N-I-6-3-1-2";

    /** 本人確認状況コード（スペース） */
    private static final String HNNN_KAKNN_JOKY_COD_SPACE = "  ";

    /** 予備（ブランク） */
    private static final String YOB_BLANK = "                 ";

    /** 引数：0番目 */
    private static final String ARGV_ZERO = "0";

    /** メッセージID：NE11001 */
    private static final String MSG_ID_NE11001 = "NE11001";

    /** 住民票生存状況表示（0：生存） */
    private static final String JMNHY_SIZN_JOKY_HYJI_SIZN = "0";

    /** SQLID SelectHnnnKaknnInf */
    private static final String SQLID_SELECT_HNNN_KAKNN_INF = "NNSI6310201.SelectHnnnKaknnInf";

    /** 生年月日（ALLゼロ） */
    private static final String BYMD_ALL_ZERO = "00000000";

    /** 取消年月日（スペース） */
    private static final String TRKSH_YMD_SPACE = "        ";

    /** トレースログ */
    private static final LogEx LOG = LogFactoryEx.getLogger(NMdI6310201BLogic.class);

    /** 障害検知（イベント）ログ */
    private static final LogEx EVENT_LOG = LogFactoryEx.getLogger(LogFactoryEx.EVENT_LOG_CATEGORY);

    /** 異常終了フラグ<ブーリアン>=false */
    private Boolean errorEndFlg = false;

    /** ファイルアクセスインスタンス<ファイルアクセスオブジェクト> */
    @Autowired
    @Qualifier("fixedFileUpdateDAO")
    private FileUpdateDAO fixedFileUpdateDAO;

    /** 年金給付住基本人確認異動情報取得要求送信ファイルパス<文字列> */
    private String nenkinFilePath = null;

    /** 年金給付住基本人確認異動情報取得要求送信ファイルライターインスタンス<ファイルライター> */
    private FileLineWriter<NI6310204FileAccessDTO> nenkinWriter = null;

    /** 異動者情報回付対象者情報ファイルパス<文字列> */
    private String idoFilePath = null;

    /** 異動者情報回付対象者情報ファイルライターインスタンス<ファイルライター> */
    private FileLineWriter<NI6310203FileAccessDTO> idoWriter = null;

    /** collectorQueryDAOフィールドの宣言 DB参照用DAO<大量参照DAO> */
    @Autowired
    @Qualifier("collectorQueryDAO")
    private DbIteratableQueryDAO collectorQueryDAO;

    // 【変数】#西暦和暦変換機能<西暦・和暦変換>
    /** 西暦和暦変換機能 */
    @Autowired
    private NSirkWrkConvUtility nSirkWrkConvUtility;

    /**
     * ジョブ前処理.
     * <p>
     * 年金給付住基本人確認異動情報取得要求送信ファイルパスを取得する.<br>
     * 異動者情報回付対象者情報ファイルパスを取得する.<br>
     * 年金給付住基本人確認異動情報取得要求送信ファイルライターインスタンスを取得する.<br>
     * 異動者情報回付対象者情報ファイルライターインスタンスを取得する.<br>
     * </p>
     * @param batchBLogicParam バッチ入力パラメータ
     * @return ビジネスロジック結果。
     */
    @SuppressWarnings("unchecked")
    @Override
    protected BatchBLogicResult doPreLogic(BatchBLogicParamEx batchBLogicParam) {

        BatchBLogicResult batchResult = CONTINUE;

        // 年金給付住基本人確認異動情報取得要求送信ファイルパスを取得する。
        // "NG00000215" + "_" + バッチ入力パラメータ.引数2（ジョブ番号） + "_SPT.dat"
        StringBuilder nenkinFileName = new StringBuilder();
        nenkinFileName.append(FILE_NAME_NG00000215);
        nenkinFileName.append(FILE_NAME_SEP);
        nenkinFileName.append(batchBLogicParam.getJobArgNm2());
        nenkinFileName.append(FILE_NAME_SEP);
        nenkinFileName.append(FILE_NAME_SPT);
        nenkinFileName.append(EXT_DAT);

        // 【基盤API】「ファイルアクセスユーティリティ」. ファイルアクセスパス取得
        // (P1)：「ファイル出力先カテゴリenum」.DIR_PATH_WORK
        // (P2)："N-I-6-3-1-2"
        // (P3)："NG00000215" + "_" + バッチ入力パラメータ.引数2（ジョブ番号） + "_SPT.dat"
        // (R) ：年金給付住基本人確認異動情報取得要求送信ファイルパス
        nenkinFilePath = FileAccessUtil.getDirPath(DirPathCategory.DIR_PATH_WORK, CASE_N_I_6_3_1_2,
            nenkinFileName.toString());

        // 異動者情報回付対象者情報ファイルパスを取得する。
        // "NEF0000206" + "_" + バッチ入力パラメータ.引数2（ジョブ番号） + "_SPT.dat"
        StringBuilder idoFileName = new StringBuilder();
        idoFileName.append(FILE_NAME_NEF0000206);
        idoFileName.append(FILE_NAME_SEP);
        idoFileName.append(batchBLogicParam.getJobArgNm2());
        idoFileName.append(FILE_NAME_SEP);
        idoFileName.append(FILE_NAME_SPT);
        idoFileName.append(EXT_DAT);

        // 【基盤API】「ファイルアクセスユーティリティ」. ファイルアクセスパス取得
        // (P1)：「ファイル出力先カテゴリenum」.DIR_PATH_WORK
        // (P2)："N-I-6-3-1-2"
        // (P3)："NEF0000206" + "_" + バッチ入力パラメータ.引数2（ジョブ番号） + "_SPT.dat"
        // (R) ：異動者情報回付対象者情報ファイルパス
        idoFilePath = FileAccessUtil.getDirPath(DirPathCategory.DIR_PATH_WORK, CASE_N_I_6_3_1_2,
            idoFileName.toString());

        // 年金給付住基本人確認異動情報取得要求送信ファイルライターインスタンスを取得する。
        // 【メソッド】ファイルライター取得
        // (P1)：年金給付住基本人確認異動情報取得要求送信ファイルパス
        // (P2)：年金給付住基本人確認異動情報取得要求送信個人番号紐付情報テーブルDTOのクラス
        // (R) ：年金給付住基本人確認異動情報取得要求送信ファイルライターインスタンス
        nenkinWriter = (FileLineWriter<NI6310204FileAccessDTO>) getFileWriter(nenkinFilePath,
            NI6310204FileAccessDTO.class);
        // 【分岐条件】【Start】バッチ入力パラメータ.引数5（ファイル出力制御区分）=="1"
        if ("1".equals(batchBLogicParam.getJobArgNm5())) {

            idoWriter = null;

        } else {
            // 【Else条件】それ以外
        // 異動者情報回付対象者情報ファイルライターインスタンスを取得する。
        // 【メソッド】ファイルライター取得
        // (P1)：異動者情報回付対象者情報ファイルパス
        // (P2)：異動者情報回付対象者情報レコード情報テーブルDTOのクラス
        // (R) ：異動者情報回付対象者情報ファイルライターインスタンス
            idoWriter = (FileLineWriter<NI6310203FileAccessDTO>) getFileWriter(idoFilePath,
                    NI6310203FileAccessDTO.class);
        }
        // 【分岐条件】【End】
        // 業務処理結果を返却する。
        // 【分岐条件】【Start】異常終了フラグ==false
        if (!errorEndFlg) {

            // 【業務ロジック返却値】
            // （終了コード）：処理継続
            batchResult = CONTINUE;

        } else {

            // 【Else条件】それ以外
            // 【業務ロジック返却値】
            // （終了コード）：異常終了
            batchResult = ERROR_END;

            // 【分岐条件】【End】
        }

        return batchResult;

    }

    /**
     * コレクタ取得処理.
     * <p>
     * 本人確認情報を取得する.
     * </p>
     * @param batchBLogicParam バッチ入力パラメータ
     * @return コレクタ。
     */
    @Override
    protected CollectorEx<SelectHnnnKaknnInfResultDTO> getCollector(
        BatchBLogicParamEx batchBLogicParam) {

        // 本人確認情報を取得する。
        // 本人確認情報取得コンディションDTOに値を設定する。
        // 「本人確認情報取得コンディションDTO」のコンストラクタを実行
        // (R)：コンディションDTO
        SelectHnnnKaknnInfConditionDTO conditionDto = new SelectHnnnKaknnInfConditionDTO();

        // コンディションDTOを設定する。
        // 被保険者ID_開始 「バッチ入力パラメータ」.引数3（被保険者ID開始）
        conditionDto.setHhknshIdStrt(batchBLogicParam.getJobArgNm3());

        // 被保険者ID_終了 「バッチ入力パラメータ」.引数4（被保険者ID終了）
        conditionDto.setHhknshIdFin(batchBLogicParam.getJobArgNm4());

        // 住民票生存状況表示_生存 "0"（生存）
        conditionDto.setJmnhySiznJokyHyjiSizn(JMNHY_SIZN_JOKY_HYJI_SIZN);

        // 住民票生存状況表示_不明 "2"（不明）
        conditionDto.setJmnhySiznJokyHyjiFmi(JMNHY_SIZN_JOKY_HYJI_FMI);

        // 被保険者保有番号種類_個人番号 "09"（個人番号）
        conditionDto.setHhknshHyuBngShuKjnbng(HHKNSH_HYU_BNG_SHU_KJNBNG);

        // 被保険者保有番号種類_基礎年金番号 "01"（基礎年金番号）
        conditionDto.setHhknshHyuBngShuKsnnknbng(HHKNSH_HYU_BNG_SHU_KSNNKNBNG);

        // 被保険者保有番号種類_仮基礎年金番号 "06"（仮基礎年金番号）
        conditionDto.setHhknshHyuBngShuKrKsnnknbng(HHKNSH_HYU_BNG_SHU_KR_KSNNKNBNG);

        // 被保険者保有番号取消年月日_個人番号 "        " スペース8桁
        conditionDto.setHhknshHyuBngTrkshYmdKjnbng(TRKSH_YMD_SPACE);

        // 被保険者保有番号取消年月日_基礎年金番号 "        " スペース8桁
        conditionDto.setHhknshHyuBngTrkshYmdKsnnknbng(TRKSH_YMD_SPACE);

        // コンディションDTOをもとに、本人確認情報取得結果DTOのコレクタインスタンスを取得する。
        // 【基盤API】「DBコレクタ」<本人確認情報取得結果DTO>のコンストラクタを実行
        // (P1)：DB参照用DAO
        // (P2)："NNSI6310201.SelectHnnnKaknnInf"
        // (P3)：コンディションDTO
        // (R) ：コレクタインスタンス
        CollectorEx<SelectHnnnKaknnInfResultDTO> collector =
            new DBCollectorEx<SelectHnnnKaknnInfResultDTO>(
                collectorQueryDAO, SQLID_SELECT_HNNN_KAKNN_INF, conditionDto);

        return collector;

    }

    /**
     * ビジネスロジック主処理.
     * <p>
     * 年金給付住基本人確認異動情報取得要求送信を出力する.
     * </p>
     * @param batchBLogicParam バッチ入力パラメータ
     * @param inputData バッチ入力データ
     * @return ビジネスロジック結果
     */
    @Override
    protected BatchBLogicResult doMainLogic(BatchBLogicParamEx batchBLogicParam,
        SelectHnnnKaknnInfResultDTO inputData) {

        // 【メソッド】#西暦和暦変換機能.和暦・西暦変換メソッド
        // (P1)：本人確認情報取得結果DTO.生年月日
        // (R)：出力生年月日
        String bymd = nSirkWrkConvUtility.wrkSirkConvUtility(inputData.getBymd());

        // 【分岐条件】【Start】出力生年月日がNULLの場合
        if (bymd == null) {

            // 出力生年月日にゼロを編集する。
            // 出力生年月日="00000000"
            bymd = BYMD_ALL_ZERO;

            // 【分岐条件】【End】
        }
        // 【分岐条件】【Start】＃異動者情報回付対象者情報ファイルライターインスタンス != NULL の場合
        if (idoWriter != null) {
        // 「異動者情報回付対象者情報レコード情報テーブルDTO」のコンストラクタを実行
        // (R)異動者情報回付対象者情報ファイル出力DTO
        NI6310203FileAccessDTO idoFileAccessDto = new NI6310203FileAccessDTO();

        // 本人確認情報取得結果DTOの情報をもとに、異動者情報回付対象者情報ファイル出力DTOを設定する。
        // 回付対象者被保険者ID 本人確認情報取得結果DTO.被保険者ID
        idoFileAccessDto.setKifTishuShHhknshId(inputData.getHhknshId());

        // 回付対象者基礎年金番号 本人確認情報取得結果DTO.基礎年金番号
        idoFileAccessDto.setKifTishuShKsnnknbng(inputData.getKsnnknbng());

        // 回付対象者個人番号 本人確認情報取得結果DTO.個人番号
        idoFileAccessDto.setKifTishuShKjnbng(inputData.getKjnbng());

        // 回付対象者加入制度コード 本人確認情報取得結果DTO.加入制度コード
        idoFileAccessDto.setKifTishuShKanySeidCod(inputData.getKanySeidCod());

        // 回付対象者生年月日 出力生年月日
        idoFileAccessDto.setKifTishuShBymd(bymd);

        // 異動者情報回付対象者情報ファイル出力DTOをもとに、異動者情報回付対象者情報ファイルを出力する。
        // 【FAO】異動者情報回付対象者情報ファイルライターインスタンス.printDataLine
        // (P1)：異動者情報回付対象者情報ファイル出力DTO
        // (R)：なし
        idoWriter.printDataLine(idoFileAccessDto);
        }
        // 【分岐条件】【End】
        // 年金給付住基本人確認異動情報取得要求送信を出力する。
        // 【変数】年金給付住基本人確認異動情報取得要求送信ファイル出力DTO<年金給付住基本人確認異動情報取得要求送信個人番号紐付情報テーブルDTO>
        // 「年金給付住基本人確認異動情報取得要求送信個人番号紐付情報テーブルDTO」のコンストラクタを実行
        // (R)年金給付住基本人確認異動情報取得要求送信ファイル出力DTO
        NI6310204FileAccessDTO nenkinFileAccessDto = new NI6310204FileAccessDTO();

        // 本人確認情報取得結果DTOの情報をもとに、年金給付住基本人確認異動情報取得要求送信ファイル出力DTOを設定する。
        // 【個人番号紐付情報】基礎年金番号に本人確認情報取得結果DTO.基礎年金番号を設定する。
        nenkinFileAccessDto.setKsnnknbng(inputData.getKsnnknbng());

        // 【個人番号紐付情報】個人番号に本人確認情報取得結果DTO.個人番号を設定する。
        nenkinFileAccessDto.setKjnbng(inputData.getKjnbng());

        // 【個人番号紐付情報】加入制度コードに本人確認情報取得結果DTO.加入制度コードを設定する。
        nenkinFileAccessDto.setKanySeidCod(inputData.getKanySeidCod());

        // 【個人番号紐付情報】生年月日に出力生年月日を設定する。
        nenkinFileAccessDto.setBymd(bymd);

        // 【本人確認情報】本人確認状況コードに"  "スペース2桁を設定する。
        nenkinFileAccessDto.setHnnnKaknnJokyCod(HNNN_KAKNN_JOKY_COD_SPACE);

        // 【個人番号紐付情報】予備に"                 "スペース17桁を設定する。
        nenkinFileAccessDto.setYob(YOB_BLANK);

        // 年金給付住基本人確認異動情報取得要求送信ファイル出力DTOをもとに、年金給付住基本人確認異動情報取得要求送信ファイルを出力する。
        // 【FAO】年金給付住基本人確認異動情報取得要求送信ファイルライターインスタンス.printDataLine
        // (P1)：年金給付住基本人確認異動情報取得要求送信ファイル出力DTO
        // (R)：なし
        nenkinWriter.printDataLine(nenkinFileAccessDto);

        // 業務処理結果を返却する。
        // 【業務ロジック返却値】
        // （終了コード）：処理継続
        return CONTINUE;

    }

    /**
     * ジョブ後処理.
     * <p>
     * 異動者情報回付対象者情報ファイルを閉じる.<br>
     * 年金給付住基本人確認異動情報取得要求送信ファイルを閉じる.<br>
     * </p>
     * @param batchBLogicParam バッチ入力パラメータ
     * @param isNormalState 例外が投げられた場合、もしくは異常終了を示すビジネスロジック結果オブジェクトが返された場合、false
     * @return ビジネスロジック結果
     */
    @Override
    protected BatchBLogicResult doPostLogic(BatchBLogicParamEx batchBLogicParam,
        boolean isNormalState) {

        BatchBLogicResult batchResult = CONTINUE;

        // 異動者情報回付対象者情報ファイルを閉じる。
        // 【分岐条件】【Start】異動者情報回付対象者情報ファイルライターインスタンスがNULLでない場合
        if (idoWriter != null) {

            // 【FAO】異動者情報回付対象者情報ファイルライターインスタンス.closeFile
            // (R)：なし
            idoWriter.closeFile();

            // 【分岐条件】【End】
        }

        // 年金給付住基本人確認異動情報取得要求送信ファイルを閉じる。
        // 【分岐条件】【Start】年金給付住基本人確認異動情報取得要求送信ファイルライターインスタンスがNULLでない場合
        if (nenkinWriter != null) {

            // 【FAO】年金給付住基本人確認異動情報取得要求送信ファイルライターインスタンス.closeFile
            // (R)：なし
            nenkinWriter.closeFile();

            // 【分岐条件】【End】
        }

        // 業務処理結果を返却する。
        // 【分岐条件】【Start】異常終了フラグ==false
        if (!errorEndFlg) {

            // 【業務ロジック返却値】
            // （終了コード）：処理継続
            batchResult = CONTINUE;

        } else {

            // 【Else条件】それ以外
            // 【業務ロジック返却値】
            // （終了コード）：異常終了
            batchResult = ERROR_END;

            // 【分岐条件】【End】
        }

        return batchResult;

    }

    /**
     * ファイルライター取得.
     * <p>
     * ファイルライターのインスタンスを取得する.
     * </p>
     * @param filePath ファイルパス
     * @param clazz 出力DTOクラス
     * @return ファイルライターインスタンス<ファイルライター>
     */
    private FileLineWriter<?> getFileWriter(String filePath, Class<?> clazz) {

        // ファイル有無を確認し、ファイルライターを取得する。
        // 【変数】ファイルインスタンス<ファイル>
        FileLineWriter<?> fileWriter = null;

        // 「ファイル」の.コンストラクタの実行
        // (P1)：ファイルパス
        // (R)：ファイルインスタンス
        File newFile = new File(filePath);

        // ファイルの存在を確認する。
        // 【分岐条件】【Start】ファイルインスタンス.exist()==true
        if (newFile.exists()) {

            // ファイルが存在する場合、トレースログ、障害検知ログを出力する。
            Map<String, String> msgMap = new HashMap<String, String>();
            msgMap.put(ARGV_ZERO, filePath);

            if (LOG.isErrorEnabled()) {

                // トレースログを出力する。
                LOG.error(MSG_ID_NE11001, msgMap);

            }

            if (EVENT_LOG.isErrorEnabled()) {

                // 【ログ】[ERROR]NE11001「ファイルパス」
                // 障害検知ログを出力する。
                EVENT_LOG.error(MSG_ID_NE11001, msgMap);

            }

            // 異常終了フラグ=true
            errorEndFlg = true;

        } else {

            // 【Else条件】それ以外
            // ファイルが存在しない場合、テキストライターインスタンスを取得する。
            // 【FAO】ファイルアクセスインスタンス.テキストライター取得
            // (P1)：ファイルパス
            // (P2)：出力DTOクラス
            // (R)：ファイルライターインスタンス
            fileWriter = fixedFileUpdateDAO.execute(filePath, clazz);

            // 【分岐条件】【End】
        }

        // 業務処理結果を返却する。
        // 【業務ロジック返却値】
        // （ファイルライター）：ファイルライターインスタンス
        return fileWriter;

    }

    /**
     * ファイルアクセスインスタンスを設定する。
     * @param fixedFileUpdateDAO ファイルアクセスインスタンス
     */
    public void setFixedFileUpdateDAO(FileUpdateDAO fixedFileUpdateDAO) {

        this.fixedFileUpdateDAO = fixedFileUpdateDAO;

    }

    /**
     * DB参照用DAOを設定する。
     * @param collectorQueryDAO DB参照用DAO
     */
    public void setCollectorQueryDAO(DbIteratableQueryDAO collectorQueryDAO) {

        this.collectorQueryDAO = collectorQueryDAO;

    }

    /**
     * nSirkWrkConvUtilityのSetter
     * @param nSirkWrkConvUtility nSirkWrkConvUtilityの設定値
     */
    public void setnSirkWrkConvUtility(NSirkWrkConvUtility nSirkWrkConvUtility) {
        this.nSirkWrkConvUtility = nSirkWrkConvUtility;
    }

}
